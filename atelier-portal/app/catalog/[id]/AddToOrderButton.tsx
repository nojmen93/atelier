'use client'

export default function AddToOrderButton() {
  return (
    <button
      onClick={() => {
        // Wired up in Phase 3
      }}
      className="w-full sm:w-auto px-8 py-2.5 rounded-md bg-foreground text-background text-sm font-medium hover:bg-neutral-200 transition"
    >
      Add to Order
    </button>
  )
}
